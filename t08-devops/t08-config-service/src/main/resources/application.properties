spring.application.name=t08-config-service

server.port=8888

# Tells Spring not to use Git (default) and to use file system (native)
spring.profiles.active=native

spring.cloud.config.server.native.search-locations=classpath:/config
